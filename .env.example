# App/DB settings
DB_HOST=localhost
DB_PORT=1001
DB_NAME=smartlog
DB_USER=smartlog_user
DB_PASSWORD=smartlog_password

# Optional: define DATABASE_URL directly to override DB_* variables
# DATABASE_URL=postgresql://smartlog_user:smartlog_password@localhost:1001/smartlog
